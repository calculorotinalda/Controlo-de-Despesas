import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.calculorotina.atelier',
  appName: 'Atelier Financeiro',
  webDir: 'dist'
};

export default config;
